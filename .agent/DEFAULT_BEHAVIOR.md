# 📌 QQS Default Agent Behavior

> **적용**: 모든 Antigravity/Claude 대화에 자동 적용

---

## 🔄 Default Workflow (모든 요청에 적용)

### Phase 1: Router (암묵적)

모든 요청 수신 시 **내부적으로** 다음을 판단:

1. **Request Type** 분류:

   - `QUICK_ANSWER`: 단순 질문 → 바로 답변
   - `IMPLEMENTATION`: 코드 변경 필요 → Validator → Implementer
   - `ETL`: 데이터 로드/변환 → Validator → Implementer → Analyst
   - `ANALYSIS`: 분석/백테스트 → Analyst
   - `VERIFICATION`: QC/검증 → Validator

2. **AS_OF_DATE** 확인 (코드 작업 시 필수)

### Phase 2: Validator (코드/ETL 작업 시)

코드 변경 전 반드시:

- [ ] PIT 규칙 준수 확인
- [ ] Look-ahead bias 가능성 검토
- [ ] 기존 테스트 영향 확인

### Phase 3: Implementer

실제 코드 작성/수정:

- [ ] 타입 힌트 사용
- [ ] 에러 핸들링
- [ ] 테스트 실행

### Phase 4: Analyst (ETL/분석 작업 시)

결과 분석:

- [ ] 데이터 품질 확인
- [ ] 예상 결과와 비교

### Phase 5: Memory Manager (작업 완료 시)

**모든 작업 완료 후** 자동 수행:

1. 중요 결정이 있었다면 → `40_Memory/decisions/` 기록
2. 새로운 학습이 있었다면 → `40_Memory/learnings/` 기록
3. 프로젝트 상태 변경 시 → `40_Memory/context/CTX-project.md` 업데이트

---

## 📋 작업 완료 시 체크리스트

모든 **비-QUICK_ANSWER** 작업 완료 시:

```
□ 테스트 통과 확인
□ Memory 기록 필요 여부 판단
  - Decision (최대 1개): 중요 설계 결정?
  - Learning (최대 1개): 버그/함정/해결책 발견?
□ 결과 요약 제공
```

---

## 🚫 예외 상황

다음 경우에는 간소화된 워크플로우 사용:

- 단순 파일 조회
- 정의/설명 요청
- 상태 확인 질문

---

## 📝 암묵적 적용 예시

### 사용자: "SIC 코드 백필해줘"

**내부 처리:**

```
[Router] Type: ETL
[Validator] PIT 영향 없음, sector_mapping 테이블 변경
[Implementer] 스크립트 실행 (backfill_sic_codes.py)
[Analyst] 결과 확인 (6102/6102 = 100%)
[Memory Manager] Learning 기록 불필요, Context 업데이트
```

**사용자에게 보이는 것:**

> SIC 백필 완료. 100% 커버리지 달성 (6102/6102).
