# E03 Command Center — 기능 SSOT v2.0 (Single Source of Truth)

- **Document Status**: `SSOT_V2.0_FINAL_DRAFT`
- **Target Strategy**: **E03 (Ensemble Vote + OFF10 + SGOV)**
- **Authoring Role**: Senior Web Designer & System Architect (PO)
- **Last Updated**: 2026-01-19 (KST)

---

## 0) 후보군 아이디어 풀 (Backlog / Candidate Pool)
> 아래는 “있으면 좋은 기능”을 **후보군으로만** 분리 정리한 목록입니다.
> 
> - **원칙**: 후보군은 언제든 넣을 수 있으나, ‘판단/실행’을 방해하지 않도록 항상 **Zone D(정보)** 또는 **옵션 모드**로 격리한다.
> - **브로커 연동 없음**을 전제로, 자동화는 “신호/계산/시각화/알림”까지만 한다.

### 0.1 입력(수동) 부담을 줄이는 기능
- 체결 입력 10초 모드(금액만 입력 → 수량/수수료/환전 자동 추정 + 정확도 배지)
- 삼성증권 체결/잔고 화면 캡처 업로드 → OCR 보조로 체결 초안 생성(사용자 확인 필수)
- 입력 누락 감지(‘실행일’인데 체결 기록 없음)
- UNKNOWN 큐(모르는 항목은 임시 저장 → 나중에 일괄 보정)
- 기록 신뢰도 점수(최근 30일 기록 완성도)

### 0.2 시뮬레이터/리플레이(Replay) 확장
- 규칙 시뮬레이터(기간/비용/세금/딜레이/슬리피지 가정)
- 파라미터 스윕(윈도우/임계값 그리드 탐색) + 오버피팅 경고 배지
- 리스크 스트레스(2008/2020/2022 등 구간 재생)
- 갭/슬리피지 스트레스(급락일 갭을 가정한 불리 체결)
- 현금흐름 시뮬(적립/일시금/분배금 재투자)
- “내 행동 대입” 시뮬(규칙대로 vs 실제 실행의 차이)

### 0.3 분석/회고(납득 가능성) 기능
- TWR/MWR(IRR)/Since My Buy 3종 성과 분리
- Deviation Cost(일탈 비용) 리포트
- MDD Autopsy(최대낙폭 구간 자동 해부)
- 회복 시간(Recovery Time) 분포
- 전환 이후 N일 성과 분포(전환 직후 흔들림 패턴)

### 0.4 행동 심리(규율 보호) 기능
- 충동 방지 쿨다운(긴급 상황에서 실행 버튼 지연)
- 위반 사유 입력 게이트(옵션)
- 감정 일기(불안/흥분/확신) + 회고 연동
- “규칙 리마인드” 문구 상황별 고정(딜레이/Hard Trigger 등)

### 0.5 알림/운영 시스템 고도화
- 알림 레벨 3단계(Info / Action / Emergency)
- 중복 알림 제거(쿨다운) + 확인 배지
- 실패 대비(푸시 실패 시 이메일/메신저 대체)

### 0.6 시각화/개인화/보안
- 상태 머신 타임라인(ON/OFF 막대)
- Execution 캘린더(실행 예정/완료/미기록)
- 위젯 재배치(Zone D 한정)
- PIN 잠금/프라이버시 마스킹/로컬-only 저장(선택)

### 0.7 리포트 자동 생성
- 월간 운영 리포트(PDF): 전환/실행/비용/세금/성과/규율
- 세금/정산용 요약(CSV/PDF)
- 연간 리캡(Year in Review)

---

## 1) 프로젝트 개요 (Executive Summary)
E03 Command Center는 **E03 전략을 ‘무결점 실행’**하기 위한 개인용 트레이딩 운영 대시보드입니다.

- **자동화 범위**: 신호 계산, 주문 산출, 리스크 경보, 기록/회고, 시뮬레이션
- **수동 범위**: 삼성증권에서 주문 실행, 체결/보유 입력(연동 불가)
- **핵심 목표**:
  1) **Rule is King**(규칙 우선) 유지
  2) 사용자의 정보 욕구는 **통제된 정보 구역(Zone D)**에서만 해소
  3) 연동 부재를 **기록 UX**로 상쇄(입력 부담 최소화)

---

## 2) 설계 철학 & 비목표(Non-goals)

### 2.1 설계 철학 (Design Philosophy)
1. **Rule is King**: 매매 신호는 오직 수학적 규칙(앙상블 MA 투표)로만 결정
2. **Decision → Execution → Information**: 사용자의 인지 흐름을 강제(Zone D는 반드시 하단)
3. **Two Truths Model**:
   - **전략 진실(자동)**: 신호/목표 비중/예상 주문
   - **현실 진실(수동)**: 실제 체결/실제 보유
4. **Fail-safe UX**: 데이터 누락/휴장/미기록이 발생해도 ‘오동작’이 아닌 ‘안전한 보류/표시’로 처리

### 2.2 비목표 (Non-goals)
- 브로커(삼성증권) 계좌/포지션/주문 **자동 연동, 자동 매매**
- 뉴스 기반 매수/매도 추천, 가격 예측, 감정적 선동(공포/탐욕 유발)
- 전략 SSOT를 변경하는 UI(룰 임의 수정은 “버전 관리”로만 허용)

---

## 3) 용어집(Glossary)
- **Verdict Date(판정일)**: 미국 정규장 마감 후(한국시간 아침) 확정된 ‘오늘의 신호’
- **Execution Date(실행일)**: **t일 신호를 t+1일에 적용**하는 실제 주문 대상일
- **ON**: 목표 비중 TQQQ 100% / SGOV 0%
- **OFF10**: 목표 비중 TQQQ 10% / SGOV 90% (비상 전환도 목표 상태는 OFF10)
- **Vote**: MA160/165/170 각각에 대해 `SMA3 > SMAwindow` 여부(✅/❌)
- **Hard Trigger(확정 트리거)**: 종가 기준으로 규칙이 ‘발동 확정’되는 트리거
- **Soft Alert(조기 경보)**: 장중 관측치로 ‘경보’만 울리는 트리거(자동 전환 아님)

---

## 4) 사용자 핵심 플로우(User Journeys)

### 4.1 Daily (매일 아침, 1분)
1) 오늘 **Verdict** 확인(ON/OFF10)
2) **내일 Execution** 존재 여부 확인
3) (있다면) Zone C에서 주문 초안 복사 → 삼성증권에서 내일 실행 준비

### 4.2 Execution Day (주문 실행일, 2분)
1) Zone C 주문 리스트 확인
2) 삼성증권에서 시장가(MOO) 주문
3) 체결 후 **체결 기록(수동)** 입력 또는 업로드
4) Compliance 상태 PASS/DELAY/UNKNOWN 갱신

### 4.3 Emergency Day (비상일)
1) Soft Alert 발생 시: “경보” 확인(규칙은 종가 기준)
2) Hard Trigger 확정 시: 다음 실행일에 OFF10 전환 수행
3) 비상일에는 UI가 ‘실수 방지 모드’(쿨다운/확인 강화)

### 4.4 신규 자금/분배금
- 현재 신호의 목표 비중에 맞추되 **부족 자산부터** 매수
- 최소 주문 미달/정수주 제약 잔액은 SGOV에 유지

---

## 5) 정보 위계 및 레이아웃(Information Architecture)

### 5.1 Zone 정의(Desktop)
| Zone | 이름 | 핵심 질문 | UX 목표 |
|---|---|---|---|
| **A** | Global Watchdog | “지금 비상인가? 데이터가 신선한가?” | 생존/정확성 즉시 파악 |
| **B** | Signal Core | “오늘 판정/내일 실행은?” | 판단 1초 인지 |
| **C** | Ops Console | “얼마를/몇 주를 주문?” | 실행 고민 제거 |
| **D** | Intel Lab | “시장은 왜 움직였나? 회고/시뮬” | 실행 후 정보 소비 |

**절대 원칙**: Zone D는 반드시 Zone B/C 아래에 위치(모바일도 동일).

### 5.2 Mobile
- Zone A(고정 바) → Zone B → Zone C → Zone D 순서를 강제
- Zone D는 기본 접힘(사용자 의도적 펼침)

---

## 6) 시간/데이터 표준(Time & Data Standards)

### 6.1 시간 표준
- 기준 자산 신호: **QQQ 미국 정규장 종가**
- 표기: **KST 기준 날짜/시간을 기본 표기**, 필요 시 ET 병기
- **Verdict 확정 시점**: 미국 정규장 마감 후(대략 KST 06:00~07:00)

### 6.2 데이터 신선도(Data Health)
- 모든 화면 상단(Zone A)에 다음 배지를 고정 표시:
  - `Data Basis: QQQ Close / Updated: YYYY-MM-DD HH:MM KST`
  - `Signal Basis: SMA3 vs SMA160/165/170`
- 데이터 누락/휴장 시:
  - 신호 계산 실패 → **전일 신호 유지(보수적)** + “DATA STALE” 경고

---

## 7) 코어 전략 로직 (Authoritative, Strategy SSOT 준수)

### 7.1 앙상블 시그널
- 각 window ∈ {160, 165, 170}에 대해
  - Vote(window) = ( **SMA(3) > SMA(window)** )
- 최종 신호:
  - ON: Vote가 **2개 이상 true**
  - OFF10: 그 외(≤1)

> **중요**: ‘현재가 vs MA’ 비교가 아니라 **MA3 vs MAwindow** 비교가 정식 정의다.

### 7.2 실행 지연(Execution Lag)
- **t일 Verdict → t+1일 Execution**
- UI는 반드시 아래를 분리 표기:
  - “오늘 판정(Verdict)”
  - “내일 실행(Execution)”

### 7.3 목표 비중(Target Allocation)
- ON: TQQQ 100% / SGOV 0%
- OFF10: TQQQ 10% / SGOV 90%

### 7.4 리밸런싱 방식
- **Delta Rebalance**: 목표 비중이 바뀔 때만 주문 생성
- 목표 비중이 동일하면 ‘주문 없음(No Action)’

### 7.5 수량 계산(정수주)
- OFF10에서 TQQQ 잔류 수량은 **수량 기준 10%를 올림(Ceiling)** 처리
  - 예: 137주 보유 → 13.7 → **14주 잔류**
- SGOV는 잔액/정수주 제약으로 인해 일부 현금이 남을 수 있음

### 7.6 신규 자금/분배금 규칙
- 현재 목표 비중 대비 **부족한 자산부터** 매수
- 최소 주문 미달은 SGOV에 축적 후 다음 실행일에 합산

---

## 8) 비상 전환(Emergency Exit) — Hard vs Soft 분리

### 8.1 Soft Alert (장중 조기 경보)
- 목적: 사용자의 심리/준비를 돕는 **경보**
- 조건(장중 관측):
  - QQQ 당일 등락률이 -7% 이하에 근접/도달(데이터 제공 시)
  - 사용자가 입력한 ‘TQQQ 평균단가’ 기준 손실이 -20% 근접/도달
- 동작:
  - Zone A 경고(색상/애니메이션) + “경보” 배지
  - **자동 전환 금지** (Hard Trigger가 확정 규칙)

### 8.2 Hard Trigger (종가 확정)
- 목적: 전략 규칙의 ‘확정 발동’
- 조건(종가 기준):
  - QQQ 당일 종가 기준 -7% 이상 하락
  - TQQQ 진입가 대비 -20% 이상 하락(사용자 기록 기반)
- 동작:
  - **다음 Execution일에 OFF10 강제 전환**을 “확정”으로 표시
  - 주문 초안을 Zone C에 자동 생성

---

## 9) 모듈 상세 명세(Functional Specs by Zone)

### 🔴 Zone A: Global Watchdog

#### MOD-A1. Data Health & Market Clock (필수)
- Updated time, Data basis(종가/장중), 휴장/조기폐장 상태
- 데이터 누락 시 “전일 유지” 및 계산 중단 사유 표시

#### MOD-A2. Emergency Monitor (필수)
- Soft Alert / Hard Trigger 분리
- Hard Trigger 확정 시: “내일 OFF10 실행 확정” 배지

#### MOD-A3. Privacy Toggle (권장)
- 금액 마스킹(****) 및 화면 공유 대비

---

### 🔵 Zone B: Signal Core

#### MOD-B1. Ensemble Vote Matrix (필수)
- 3개 투표 카드:
  - MA160: ✅/❌ + (SMA3 - SMA160) 여유(%)
  - MA165: ✅/❌ + 여유(%)
  - MA170: ✅/❌ + 여유(%)
- Verdict 배지: `STRONG ON` / `OFF10` (문구는 단정적으로, 감정 없이)

#### MOD-B2. Verdict vs Execution Card (필수)
- “오늘 판정(Verdict)”과 “내일 실행(Execution)”을 동시에 표시
- 실행 필요 시, Zone C로 스크롤 유도(‘주문 있음’)

#### MOD-B3. Pre-Close Scenario Sandbox (선택)
- 슬라이더로 “가정 종가”를 바꿔서 내일 신호가 어떻게 바뀌는지 **심리적 대비용** 미리보기
- 경고 문구 고정:
  - “이 기능은 예측이 아니라 가정 시뮬레이션입니다. 최종 판정은 종가 확정 후 결정됩니다.”

---

### 🟢 Zone C: Ops Console

#### MOD-C1. Smart Order Calculator (필수)
- 입력:
  - (선택) 총자산(또는 계좌 평가액)
  - (권장) 현재 보유 수량(TQQQ/SGOV) 또는 ‘최근 스냅샷’
- 출력:
  - ON→OFF10 전환 주문
  - OFF10→ON 전환 주문
  - 신규자금/분배금 투입 주문(부족 자산 우선)
- 주문 포맷:
  - “TQQQ 시장가 매도 N주”
  - “SGOV 시장가 매수 M주”
  - Copy Order 버튼(삼성증권 입력용)
- 수량 규칙:
  - OFF10 잔류 TQQQ 수량은 올림
  - SGOV 매수는 잔액/버퍼 고려(기본 0.2% 현금 완충)

#### MOD-C2. Holdings & Reconciliation (필수)
- “전략 목표” vs “현실 보유(수동)”를 한 화면에서 비교
- 상태:
  - PASS: 목표와 일치
  - DELAY: 실행이 늦어짐(사용자 체크)
  - UNKNOWN: 보유 입력 없음

#### MOD-C3. Tax Jar (선택/고급)
- FIFO 기반 tax lots 관리(수동 체결 입력 기반)
- UI: “세금 잠금 구간(회색 해치)” + 툴팁
- 주의 문구:
  - “세금 계산은 가정/추정이며 법적/세무 자문이 아닙니다. 공제/세율은 설정에서 조정 가능합니다.”

---

### 🟣 Zone D: Intel Lab (정보/회고/시뮬)

#### MOD-D1. AI Daily Briefing (권장)
- 원칙:
  - 팩트(금리/실적/지표)만 요약
  - 예측/매수 권유 금지
- UI 고정 문구:
  - “News is Noise. Trust the Signal.”
- 출처:
  - 헤드라인/소스 링크는 접힘 카드로 제공
- 실패 폴백:
  - “오늘 브리핑 생성 실패 → 헤드라인만 표시”

#### MOD-D2. Compliance Lawn (권장)
- GitHub 잔디 스타일
- 상태 4종:
  - Green(PASS), Red(VIOLATION), Yellow(DELAY), Gray(UNKNOWN)
- “연속 준수” 배지(도파민 보상)

#### MOD-D3. Underwater Plot (권장)
- 전고점 대비 낙폭(%)만 보여주는 차트
- 메시지(선택): “과거 유사 낙폭의 평균 회복 기간” (단, ‘예측’이 아닌 통계)

#### MOD-D4. Simulator & Replay Lab (권장/확장)
- 기간/비용/세금/딜레이 가정 기반 시뮬
- 위기구간 리플레이
- 결과는 항상 “가정 기반” 배지 표기

#### MOD-D5. Reports (선택)
- 월간/연간 리포트(PDF)
- 로그/체결/신호/성과 내보내기(CSV)

---

## 10) 데이터 모델(권장) — Postgres/Supabase 기준
> 브로커 연동이 없으므로 **사용자 입력 테이블**이 핵심입니다.

```sql
-- 1) 일별 종가(신호 계산용)
CREATE TABLE prices_daily (
  date DATE PRIMARY KEY,
  symbol TEXT NOT NULL, -- 'QQQ', 'TQQQ', 'SGOV'
  close NUMERIC,
  source TEXT,
  updated_at TIMESTAMPTZ
);

-- 2) 일별 신호/투표 결과
CREATE TABLE signals_daily (
  date DATE PRIMARY KEY,
  verdict TEXT NOT NULL, -- 'ON' | 'OFF10'
  vote_160 BOOLEAN,
  vote_165 BOOLEAN,
  vote_170 BOOLEAN,
  margin_160 NUMERIC,
  margin_165 NUMERIC,
  margin_170 NUMERIC,
  execution_date DATE, -- t+1
  computed_at TIMESTAMPTZ
);

-- 3) 사용자 보유 스냅샷(수동 입력)
CREATE TABLE holdings_snapshot (
  as_of_date DATE PRIMARY KEY,
  tqqq_qty NUMERIC,
  tqqq_avg_cost NUMERIC,
  sgov_qty NUMERIC,
  cash_usd NUMERIC,
  notes TEXT,
  created_at TIMESTAMPTZ
);

-- 4) 체결 기록(수동 입력/업로드)
CREATE TABLE trades (
  trade_id UUID PRIMARY KEY,
  trade_time TIMESTAMPTZ,
  symbol TEXT NOT NULL,
  side TEXT NOT NULL, -- 'BUY'|'SELL'
  qty NUMERIC NOT NULL,
  price NUMERIC NOT NULL,
  fee NUMERIC DEFAULT 0,
  fx_rate NUMERIC,
  memo TEXT
);

-- 5) FIFO 세금용 랏(옵션)
CREATE TABLE tax_lots (
  lot_id UUID PRIMARY KEY,
  symbol TEXT NOT NULL,
  buy_date DATE NOT NULL,
  qty NUMERIC NOT NULL,
  cost_basis NUMERIC NOT NULL,
  remaining_qty NUMERIC NOT NULL,
  is_active BOOLEAN DEFAULT TRUE
);

-- 6) 감사/규율
CREATE TABLE daily_audit (
  date DATE PRIMARY KEY,
  verdict TEXT,
  execution_date DATE,
  compliance TEXT, -- PASS|VIOLATION|DELAY|UNKNOWN
  tax_liability_est NUMERIC,
  notes TEXT
);

-- 7) AI 브리핑(옵션)
CREATE TABLE ai_briefings (
  date DATE PRIMARY KEY,
  summary TEXT,
  sources JSONB,
  created_at TIMESTAMPTZ
);
```

---

## 11) API 설계(권장)
- `GET /api/signal/current` : 오늘 Verdict + 내일 Execution + 투표/여유
- `GET /api/signal/history?from=&to=` : 기간 신호 타임라인
- `POST /api/orders/recommend` : (보유/자산 입력) → 주문 리스트 생성
- `POST /api/holdings/snapshot` : 보유 스냅샷 저장
- `POST /api/trades` / `GET /api/trades` : 체결 기록
- `GET /api/audit` : Compliance 잔디/리포트
- `GET /api/briefing/today` : AI 브리핑
- `POST /api/sim/run` : 시뮬레이터 실행(옵션)

---

## 12) 품질 게이트(최소 Acceptance Criteria)
1) MOD-B1의 비교는 반드시 **SMA3 vs SMAwindow**
2) Verdict Date와 Execution Date를 **항상 분리 표기**
3) Emergency는 **Soft Alert(장중) / Hard Trigger(종가)**로 분리
4) 브로커 연동이 없으므로 Compliance는 **PASS/VIOLATION/DELAY/UNKNOWN** 지원
5) 데이터 누락 시: **전일 신호 유지 + DATA STALE 표시** (침묵 금지)
6) AI 브리핑은 **예측/매수 권유 금지**, 실패 시 폴백 제공

---

## 13) 구현 로드맵(권장)
- **Phase 1 (Survival Core)**: Zone A1/A2 + Zone B1/B2 + Zone C1/C2
- **Phase 2 (Ops & Audit)**: trades/holdings/audit 저장 + Compliance 잔디 + Export
- **Phase 3 (Intel & Simulator)**: AI Briefing + Underwater Plot + Simulator/Replay

